<Application x:Class="SampleWpfApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SampleWpfApp"
             xmlns:core="clr-namespace:SampleWpfApp.Core"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <SolidColorBrush x:Key="ControlTextBrush"
                         Color="{x:Static SystemColors.ControlTextColor}"
        />

        <SolidColorBrush x:Key="ControlTextHighlightBrush"
                         Color="{x:Static SystemColors.HighlightTextColor}"
                         />

        <SolidColorBrush x:Key="ControlBrush"
                         Color="{x:Static SystemColors.ControlColor}"
                         />

        <SolidColorBrush x:Key="ControlDarkBrush"
                         Color="{x:Static SystemColors.ControlDarkColor}"
                         />

        <SolidColorBrush x:Key="ControlDarkDarkBrush"
                         Color="{x:Static SystemColors.ControlDarkDarkColor}"
                         />

        <SolidColorBrush x:Key="HighlightBrush"
                         Color="{x:Static SystemColors.HighlightColor}"
                         />

        <SolidColorBrush x:Key="ButtonHighlightBrush"
                         Color="{x:Static SystemColors.HighlightTextColor}"
                         />

        <SolidColorBrush x:Key="ButtonFaceBrush"
                         Color="White"
                         />

        <SolidColorBrush x:Key="ActionButtonIdle"
                         Color="#00C000"
                         />

        <SolidColorBrush x:Key="ActionButtonBusy"
                         Color="#C00000"
                         />

        <SolidColorBrush x:Key="ActionButtonDisabled"
                         Color="#808080"
                         />

        <Style x:Key="BaseTextStyle" TargetType="Control">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Foreground" Value="{StaticResource ControlTextBrush}" />
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
        </Style>

        <Style TargetType="Frame">
            <Setter Property="Background" Value="{StaticResource ActionButtonDisabled}"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="UseLayoutRounding" Value="True"/>
        </Style>

        <Style TargetType="Label" BasedOn="{StaticResource BaseTextStyle}" />
        <Style TargetType="TextBox" BasedOn="{StaticResource BaseTextStyle}" />
        <Style TargetType="Button" BasedOn="{StaticResource BaseTextStyle}" />
        <Style TargetType="ComboBox" BasedOn="{StaticResource BaseTextStyle}" />
        <Style TargetType="ListBox" BasedOn="{StaticResource BaseTextStyle}" />
        <Style TargetType="MenuItem" BasedOn="{StaticResource BaseTextStyle}" />

        <Style x:Key="ImageLogoStyle" TargetType="Image">
            <Setter Property="Width" Value="311"/>
            <Setter Property="Height" Value="119"/>
            <Setter Property="Margin" Value="0,1,0,2"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style x:Key="ImageOnlyButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="144"/>
            <Setter Property="Height" Value="119"/>
            <Setter Property="Margin" Value="0,1,0,2"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFEFEFEF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StartingTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="48"/>
            <Setter Property="Foreground" Value="{StaticResource ControlTextBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style TargetType="StatusBar" BasedOn="{StaticResource BaseTextStyle}">
            <Setter Property="Background" Value="{StaticResource ControlDarkDarkBrush}"/>
            <Setter Property="Height" Value="50"/>
        </Style>

        <Style TargetType="StatusBarItem" BasedOn="{StaticResource BaseTextStyle}">
            <Setter Property="Foreground" Value="{StaticResource ControlBrush}"/>
            <Setter Property="Padding" Value="10 0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style x:Key="LabelTitleBarStyle" TargetType="Label" BasedOn="{StaticResource BaseTextStyle}">
            <Setter Property="Background" Value="{StaticResource HighlightBrush}"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Foreground" Value="{StaticResource ButtonHighlightBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10 0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LabelCardStyle" TargetType="Label" BasedOn="{StaticResource BaseTextStyle}">
            <Setter Property="Background" Value="{StaticResource HighlightBrush}"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Foreground" Value="{StaticResource ButtonHighlightBrush}"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LabelStatusBarStyle" TargetType="Label" BasedOn="{StaticResource BaseTextStyle}">
            <Setter Property="Background" Value="{StaticResource ButtonFaceBrush}"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button" BasedOn="{StaticResource BaseTextStyle}">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding ButtonActionState}" Value="{x:Static core:ActionState.Idle}">
                    <Setter Property="Background" Value="{StaticResource ActionButtonIdle}"/>
                    <Setter Property="IsEnabled" Value="True"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ButtonActionState}" Value="{x:Static core:ActionState.Busy}">
                    <Setter Property="Background" Value="{StaticResource ActionButtonBusy}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding ButtonActionState}" Value="{x:Static core:ActionState.Disabled}">
                    <Setter Property="Background" Value="{StaticResource ActionButtonDisabled}"/>
                    <Setter Property="IsEnabled" Value="False"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BorderReadingStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource ControlDarkDarkBrush}" />
        </Style>

        <Style x:Key="TextBlockReadingStyle" TargetType="TextBlock">
            <Setter Property="Background" Value="{StaticResource ControlDarkDarkBrush}"/>
            <Setter Property="FontSize" Value="64"/>
            <Setter Property="Foreground" Value="{StaticResource ControlTextHighlightBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

    </Application.Resources>
</Application>
